import { Layout } from "antd";

const MainPage = () => {
  return (
    <Layout style={{ display: "flex", flex: 1, minHeight: "100vh" }}>
      Main Page
    </Layout>
  );
};

export default MainPage;
